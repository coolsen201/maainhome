## Packages
framer-motion | For smooth UI transitions and animations
lucide-react | Iconography (already in base, but ensuring availability)

## Notes
WebSocket connection required at /ws path.
WebRTC requires STUN/TURN servers for real-world usage, but local network might work with just STUN.
Camera and Microphone permissions will be requested immediately on the Home Station page.
